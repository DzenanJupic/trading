use clap::ArgMatches;
use parse_algorithms;

use crate::init::Action;
use crate::init::settings::ConfigFile;

pub fn parse_start(args: &ArgMatches, current_settings: &mut ConfigFile) -> Action {
    // Action::Start()
    unimplemented!()
}

#[]
mod algorithms {}
